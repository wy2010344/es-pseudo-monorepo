# Git 钩子和提交规范

本 pseudo-monorepo 项目使用现代化的提交工具来确保代码质量和提交规范。

## 🏗️ 项目结构

- **顶层项目**: 使用 Commitizen + Commitlint 的现代化提交流程
- **子包项目**: 独立的 Git 仓库，直接使用 `git commit`

## 🚀 现代化工具栈

### Commitizen

- 交互式提交界面
- 自动生成符合规范的提交信息
- 支持 Conventional Commits 标准

### Commitlint

- 自动验证提交信息格式
- 基于 `@commitlint/config-conventional`
- 确保提交历史的一致性

### Lint-staged

- 只对暂存文件进行处理
- 自动格式化代码
- 提升提交质量

## 📝 提交信息规范

采用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

## 🛠️ 使用方法

### 顶层项目提交

#### 推荐方式 - Commitizen

```bash
pnpm commit
```

这会启动交互式提交界面，引导你创建规范的提交信息。

#### 传统方式

```bash
git add .
git commit -m "feat: add new feature"
```

### 子包项目提交

```bash
# 进入子包目录
cd packages/wy-helper/wy-helper

# 正常 Git 操作
git add .
git commit -m "feat: add utility function"
git push
```

## 🔧 配置文件

- `commitlint.config.js` - Commitlint 配置
- `.lintstagedrc.js` - Lint-staged 配置
- `.husky/` - Git 钩子
- `package.json` - Commitizen 配置

## 💡 最佳实践

1. **使用 `pnpm commit`** 来获得最佳的提交体验
2. **让工具引导你** 创建规范的提交信息
3. **子包项目保持独立** 不受顶层工具影响
4. **利用自动化** 减少手动操作和错误

## 🎯 工作流程

1. 修改代码
2. `git add .` 暂存文件
3. `pnpm commit` 交互式提交
4. 工具自动处理格式化和验证
5. 生成规范的提交信息
